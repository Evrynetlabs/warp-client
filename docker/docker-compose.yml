version: '3'
services:
  warp:
    build:
      context: ../
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    environment:
      PORT: 3000
      HOST: 0.0.0.0
      NODE_ENV: development
      BABEL_ENV: development
    volumes:
      - ../:/app
      - /app/node_modules/